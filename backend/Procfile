web: gunicorn main:app --worker-class uvicorn.workers.UvicornWorker --workers ${WEB_CONCURRENCY:-4} --bind 0.0.0.0:${PORT:-8000} --timeout 120 --forwarded-allow-ips '*'
worker: celery -A celery_app worker --loglevel=info --concurrency=${CELERY_CONCURRENCY:-2}
release: alembic upgrade head
